# Environment variables template
# Copy to .env.local and fill with your actual values
# NEVER commit .env.local to version control

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Upstash Redis Configuration (Required for API rate limiting - Phase 1 Security)
# Sign up at https://upstash.com/ and create a Redis database
# Critical for protecting against API abuse and excessive OpenAI costs
UPSTASH_REDIS_REST_URL=your-upstash-redis-rest-url
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-rest-token

# EmailJS Configuration (Required for contact form)
# Sign up at https://emailjs.com/ and create an email service
# Used in EnhancedContact.tsx for client-side email sending
NEXT_PUBLIC_EMAILJS_SERVICE_ID=your_service_id
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=your_template_id
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=your_public_key

# Supabase Configuration (Optional)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Email Service (Optional - Alternative to EmailJS)
EMAIL_SERVICE_API_KEY=your-email-service-key

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://ahmedziwar.com
NEXT_PUBLIC_GA_ID=your-ga-id